<?

	require("includes/phpmailer.inc.php");
	
	/*
if($_POST)
	{
		$name = 	$_POST["name"];
		$sender = 	$_POST["email"];
		$message = 	$_POST["message"];
		
		$mail = new phpmailer;
		
		$mail->IsSMTP(); // set mailer to use SMTP
		$mail->From = "$sender";
		$mail->FromName = "$name";
		$mail->Host = "localhost";
		// $mail->Host = "smtp1.site.com;smtp2.site.com";  // specify main and backup server
		$mail->AddAddress("me@derrylcarter.com", "Derryl Carter");
		$mail->WordWrap = 50;    // set word wrap
		
		$mail->IsHTML(true);    // set email format to HTML
		$mail->Subject = "Message from DerrylCarter.com";
		$mail->Body = "$message";
		$mail->Send(); // send message
	
	}
*/

?>